apiVersion: v1
kind: Service
metadata:
  name: playback-service
  namespace: streaming
  labels:
    app: playback-service
spec:
  type: ClusterIP
  selector:
    app: playback-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP
