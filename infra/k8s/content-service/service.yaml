apiVersion: v1
kind: Service
metadata:
  name: content-service
  namespace: streaming
  labels:
    app: content-service
spec:
  type: ClusterIP
  selector:
    app: content-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
